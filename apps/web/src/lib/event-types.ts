/**
 * Re-export of the EventData type from `@atmo-dev/events-ui`. The package owns
 * the canonical structural type so the UI components and the consumer app
 * agree on the shape of an event record.
 */
export type { EventData } from '@atmo-dev/events-ui';
